<div class="row">
  <aside class="col-md-4">
    <div class="card border-primary mb-3">
      <div class="card-body">
        <h3 class="card-title"><%= @user.name %></h3>
      </div>
      <div class="card-body">
        <div class="user_image">
          <% if @user.image? %>
            <%= image_tag(@user.image.url) %>
          <% end %>
        </div>
      </div>
    </div>
    <div>
      <% if current_user == @user %>
        <%= link_to 'プロフィール編集', edit_user_path(@user),class: 'btn btn-primary btn-block' %>
      <% end%>
    </div>
  </aside>
  <div class="col-md-8">
    <ul class="nav nav-tabs">
      <li class="nav-item">
        <a class="nav-link active show" data-toggle="tab" href="#home">投稿一覧</a>
      </li>
      <li class="nav-item">
        <a class="nav-link active show" data-toggle="tab" href="#home">フォロー中</a>
      </li>
      <li class="nav-item">
        <a class="nav-link active show" data-toggle="tab" href="#home">フォロワー</a>
      </li>
    </ul>
  </div>
</div>